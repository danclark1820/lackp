<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Team Simpson Slammers</title>
  </head>

  <body>
    <h1>Simpson Slammers</h1>


    <ul>
      <h4>Player: Position</h4>
      <% @lackp.each do |key, value| %>
        <% if value[:team] == "Simpson Slammers" %>
          <li> <%= value[:first_name] %> <%= value[:last_name] %>: <%= value[:position] %> </li>
        <% end %>
      <% end %>
      <li><a href="/">Back Home</a></li>
    </ul>
  </body>
</html>
