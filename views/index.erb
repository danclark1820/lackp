<!DOCTYPE html>
<html lang="en">
  <head>
    <title>League of Cool Kickball Professionals</title>
    <link rel="stylesheet" href="home.css">
  </head>

  <body>
    <h1>Team List</h1>
    <ul>
      <% @unique_teams.each do |x| %>
        <a href="/<%= x%>"<li><%= x %></li></a><br>
      <% end %>
    </ul>

    <h1>Position List</h1>
    <ul>
      <% @unique_positions.each do |y| %>
        <a href="/position/<%= y %>"<li><%= y %></li></a><br>
      <% end %>
    </ul>

  </body>
</html>
